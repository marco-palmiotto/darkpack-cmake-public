name: Test darkpack

on:
  pull_request:
    branches:
      - main
  
  push:
    branches:
      - main

jobs:
  test:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        distro: ['fedora36', 'debian']
    
    steps:
    - name: Cloning the last commit
      uses: actions/checkout@v2

    - name: List files in the workspace
      run: ls ${GITHUB_WORKSPACE}

    - name: Create build directory
      run: |
        mkdir -p ${GITHUB_WORKSPACE}/build
        chmod -R 777 ${GITHUB_WORKSPACE}/build
    

    - name: Set up Docker
      uses: docker/setup-docker-action@v4

    - name: Pull the docker images
      run: docker pull docker.io/marcopalmiotto/darkpack-deploy:${{ matrix.distro }}

    - name: Run tests in ${{ matrix.distro }} container
      run: docker run --mount type=bind,src=${GITHUB_WORKSPACE},target=~/darkpack,U=true -w /darkpack docker.io/marcopalmiotto/darkpack-deploy:${{ matrix.distro }} /bin/bash -c "./run_CI.sh"
           
